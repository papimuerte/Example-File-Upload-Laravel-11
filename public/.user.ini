memory_limit = 512M
post_max_size: 500M
upload_max_filesize: 500M
max_execution_time: 100
; Ensure errors are logged
log_errors = On

; Direct errors to stderr (App Platform captures this)
error_log = stderr

; Set the desired error reporting level (e.g., all errors)
error_reporting = E_ALL
